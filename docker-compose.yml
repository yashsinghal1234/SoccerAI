version: '3.8'
services:
  postgres:
    image: postgres:15
    container_name: soccer-ai-postgres
    environment:
      POSTGRES_USER: user
      POSTGRES_PASSWORD: password
      POSTGRES_DB: soccer_ai
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

  redis:
    image: redis:7
    container_name: soccer-ai-redis
    ports:
      - "6379:6379"

volumes:
  postgres_data:
